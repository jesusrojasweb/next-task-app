import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#17181f",
    },
    secondary: {
      main: "#fa77a1",
    },
    text: {
      main: "#fff",
    },
    error: {
      main: "#ea4c89",
    },
  },
});

export default theme;
